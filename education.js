export const cases=[['低位启动','历史低位、重新站上MA20、量能温和放大。'],['突破回踩','突破压力后回踩不破，适合看确认而不是第一时间乱追。'],['历史新高','大位置高位，上方压力不清晰，但波动和回撤风险加大。'],['放量滞涨','成交量放大但价格不涨，说明抛压可能很重。'],['缩量回调','上涨趋势中的健康休息，关键看是否守住MA20。'],['下降趋势反弹','反弹不等于反转，需要重新站上MA20/MA60。'],['箱体震荡','箱体下沿低吸，上沿减仓，中间位置不占优势。'],['假突破','突破后快速跌回压力位下方，容易套追高资金。'],['主升浪','价格强于MA20，MA20强于MA60，并持续放量。']];
export function match(d,p){if(p.big==='历史高位'&&d.volume==='breakout')return'历史新高';if(d.volume==='stagnation')return'放量滞涨';if(d.volume==='shrinkPullback'&&d.trend==='up')return'缩量回调';if(d.trend==='down')return'下降趋势反弹';if(d.trend==='sideways')return'箱体震荡';if(p.big==='历史低位'&&d.price>d.ma20)return'低位启动';if(d.price>d.ma20&&d.ma20>d.ma60&&d.trend==='up')return'主升浪';return'突破回踩/继续观察'}
export function mistakes(d,p){let m=['不要只看一个指标，位置、趋势、量能要一起看。'];if(p.big==='历史高位')m.push('历史高位时，MA20附近也不等于低位。');if(d.volume==='breakout')m.push('放量突破不是无脑买点，突破后能否站稳更重要。');if(d.trend==='down')m.push('下降趋势里的上涨，很多只是反弹，不一定是反转。');if(d.positionState==='holdingLoss')m.push('亏损时不要为了回本而失去纪律。');return m}
export function render(el){el.innerHTML=cases.map(c=>`<div class="case"><b>${c[0]}</b><span>${c[1]}</span></div>`).join('')}
