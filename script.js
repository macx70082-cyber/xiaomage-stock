function analyze(){let p=+price.value,m=+ma20.value,h=+high.value,l=+low.value,b=+buy.value,q=+qty.value;if([p,m,h,l,b,q].some(v=>!v)){result.textContent='请填写完整数据';return;}let pos=(p-l)/(h-l),big=pos<0.33?'低位':pos<0.66?'中位':'高位';let d=(p-m)/m*100,small=Math.abs(d)<3?'接近MA20':d>0?'高于MA20':'低于MA20';let cost=b*q,val=p*q,profit=val-cost,rate=profit/cost*100;result.textContent=`股票：${name.value} (${code.value})\n\n大位置：${big}\n小位置：${small}\nMA20偏离：${d.toFixed(2)}%\n\n成本：${cost.toFixed(2)} 元\n市值：${val.toFixed(2)} 元\n盈亏：${profit.toFixed(2)} 元\n收益率：${rate.toFixed(2)}%\n\nAI分析：当前属于${big}，${small}。`;}