export function plan(d,sr,r){const support=sr.support.v?sr.support.v.toFixed(2):'关键支撑';const res=sr.resistance.v?sr.resistance.v.toFixed(2):'新高确认位';let arr=[];if(d.positionState==='watching'){arr.push(`低吸方案：回踩 ${support} 附近不破，并出现企稳信号，再考虑观察。`);arr.push(`突破方案：有效突破 ${res} 且不是放量滞涨，再考虑跟踪。`)}else if(d.positionState==='holdingProfit'){arr.push(`已有盈利：可以用 MA20 或 ${support} 作为防守线，跌破就降低仓位。`);arr.push('不要把盈利单做成亏损单，越接近高位越要重视分批止盈。')}else{arr.push(`已有亏损：先确认是否跌破 ${support} 或 MA20，跌破后不要靠幻想补仓。`);arr.push('亏损单优先控制风险，而不是急着摊低成本。')}arr.push(`止损/放弃条件：跌破 ${support} 后无法快速收回，或出现放量滞涨、量价背离。`);if(r.grade==='D'||r.grade==='E')arr.unshift('当前风险偏高，不适合无计划追入。');return arr}
